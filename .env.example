# Values below match the emulator the suite is verified against.
# Override per machine in your own .env (which is gitignored).
ANDROID_DEVICE_NAME=emulator-5554
ANDROID_PLATFORM_VERSION=16
ANDROID_APP_PATH=./apps/MyDemoApp.apk
ANDROID_UDID=

WDIO_LOG_LEVEL=warn
